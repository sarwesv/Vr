// Empty translation unit: this target exists only to expose
// CGVirtualDisplayPrivate.h to Swift. The classes it declares already exist
// at runtime inside CoreGraphics.framework, so there's nothing to implement
// here — just a source file so SwiftPM accepts this as a valid target.
#import "include/CGVirtualDisplayPrivate.h"
